#include <iostream>

using namespace std;

int main() {
    int arr[] = {4, 2, 1, 1, 5};
    int n = sizeof(arr) / sizeof(arr[0]);
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];
    }

    cout << "Jumlah semua elemen dalam array: " << sum << endl;

    return 0;
}
